{/*let produtos = [];

const getProdutos = () => {
  return produtos;
};

const adicionarProduto = (tipo, descricao, preco, medida, modelo, quantidadeEstoque) => {
  const produto = { 
    id: produtos.length + 1, 
    marca: 'Piremaxx', 
    tipo, 
    descricao, 
    preco, 
    medida, 
    modelo, 
    quantidadeEstoque 
  };
  produtos.push(produto);
  return produto;
};

const atualizarProduto = (id, tipo, descricao, preco, medida, modelo, quantidadeEstoque) => {
  const produto = produtos.find(produto => produto.id === id);
  if (produto) {
    produto.tipo = tipo;
    produto.descricao = descricao;
    produto.preco = preco;
    produto.medida = medida;
    produto.modelo = modelo;
    produto.quantidadeEstoque = quantidadeEstoque;
  }
  return produto;
};

const deletarProduto = (id) => {
  produtos = produtos.filter(produto => produto.id !== id);
  return produtos;
};

export default { getProdutos, adicionarProduto, atualizarProduto, deletarProduto };*/}
// parte da tentativa de utilizar o mongoDb guardada para caso de ser utilizada no futuro servindo de codigo exemplo
