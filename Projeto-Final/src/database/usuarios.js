{/*let usuarios = [];

const getUsuarioPorNome = (nomeUsuario) => {
  return usuarios.find(usuario => usuario.nomeUsuario === nomeUsuario);
};

const adicionarUsuario = (nomeUsuario, senha, role = 'user') => {
  const usuario = { nomeUsuario, senha, role };
  usuarios.push(usuario);
  return usuario;
};

const atualizarUsuario = (nomeUsuario, novoNomeUsuario, novaSenha, novoRole) => {
  const usuario = usuarios.find(usuario => usuario.nomeUsuario === nomeUsuario);
  if (usuario) {
    usuario.nomeUsuario = novoNomeUsuario;
    usuario.senha = novaSenha;
    usuario.role = novoRole || usuario.role; 
  }
  return usuario;
};

export default { getUsuarioPorNome, adicionarUsuario, atualizarUsuario };*/}
// parte da tentativa de utilizar o mongoDb guardada para caso de ser utilizada no futuro servindo de codigo exemplo
